doctype html
include _env/_env
include pug/_funcs
include pug/_snippets

-
  title = 'title';

  keywords = [
  ];

  description = '';

  cssFiles = [
    'static/style/main.scss',
  ];

+htmlStartTag(env)
head
  +header(env)
  title= title
  + meta('keywords', keywords)
  + meta('description', description)
  each cssFile in cssFiles
    + css(cssFile)
body
  .container hello world!

  +polyfill(env)
  +jquery(env)

+htmlEndTag(env)
